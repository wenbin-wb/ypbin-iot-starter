<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.ypbin</groupId>
        <artifactId>ypbin-iot-starter</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>ypbin-iot-dependencies</artifactId>
    <packaging>pom</packaging>

    <name>${project.artifactId}</name>
    <description>IoT 模块统一版本管理，同时作为全部 IoT 子模块的 parent</description>

    <dependencyManagement>
        <dependencies>
            <!-- Netty BOM：协议模块只引 BOM，不写 artifact 版本（PROTOCOLS §4.7） -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- 协议库：Modbus（EPL-2.0，基于 Netty，异步 API） -->
            <dependency>
                <groupId>com.digitalpetri.modbus</groupId>
                <artifactId>modbus-tcp</artifactId>
                <version>${modbus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.digitalpetri.modbus</groupId>
                <artifactId>modbus-serial</artifactId>
                <version>${modbus.version}</version>
            </dependency>

            <!-- 协议库：MQTT（Apache-2.0 客户端 + 仅测试用的嵌入式 broker） -->
            <dependency>
                <groupId>com.hivemq</groupId>
                <artifactId>hivemq-mqtt-client</artifactId>
                <version>${hivemq.mqtt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.moquette</groupId>
                <artifactId>moquette-broker</artifactId>
                <version>${moquette.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- 协议库：OPC UA（MPL-2.0 / EPL-2.0）。
                 注意：服务端传输实现不在 milo-sdk-server 里，而在独立的 milo-transport 制品 —— 这点按名字猜会找不到类 -->
            <dependency>
                <groupId>org.eclipse.milo</groupId>
                <artifactId>milo-sdk-client</artifactId>
                <version>${milo.version}</version>
            </dependency>
            <dependency>
                <groupId>org.eclipse.milo</groupId>
                <artifactId>milo-sdk-server</artifactId>
                <version>${milo.version}</version>
                <scope>test</scope>
            </dependency>
            <!-- 服务端传输实现（Netty）在独立制品里；客户端用到 4 参 create 重载时也需要它 compile 可见 -->
            <dependency>
                <groupId>org.eclipse.milo</groupId>
                <artifactId>milo-transport</artifactId>
                <version>${milo.version}</version>
            </dependency>

            <!-- 本仓模块 -->
            <dependency>
                <groupId>cn.ypbin</groupId>
                <artifactId>ypbin-iot-core</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>cn.ypbin</groupId>
                <artifactId>ypbin-iot-runtime</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>cn.ypbin</groupId>
                <artifactId>ypbin-iot-transport</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>cn.ypbin</groupId>
                <artifactId>ypbin-iot-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>cn.ypbin</groupId>
                <artifactId>ypbin-iot-protocol-tcp</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>cn.ypbin</groupId>
                <artifactId>ypbin-iot-protocol-modbus</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>cn.ypbin</groupId>
                <artifactId>ypbin-iot-protocol-mqtt</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>cn.ypbin</groupId>
                <artifactId>ypbin-iot-protocol-opcua</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>cn.ypbin</groupId>
                <artifactId>ypbin-iot-test</artifactId>
                <version>${revision}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- 测试基座：全部模块统一 JUnit 5 + AssertJ -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
