# 安全政策（Security Policy）

感谢你帮助让 `ypbin-iot-starter` 更安全。

## 支持范围

本项目**尚未发布任何正式版本**（开发版 `0.1.0-SNAPSHOT`，Maven Central 上无此坐标），
因此**没有「受支持的发布版本」**。当前按以下方式处理：

| 版本 | 是否提供安全修复 |
|---|---|
| `master`（当前唯一分支） | ✅ 尽力修复（修复后进 `master`，并在 `CHANGELOG.md` 记录） |
| 已发布的正式版 | 暂无（尚未发布任何版本） |

> 本仓的状态是**早期开发**：现有证据全部来自仓库自带的模拟器 / 嵌入式 broker / OPC UA 服务端，
> **尚未在真实项目中接入验证**。请按「可以读、可以试、不要直接上生产」看待它。

## 如何上报漏洞

**请不要通过公开 Issue 上报安全问题**，以免在修复前被利用。请任选一种私密渠道：

1. GitHub 私密漏洞上报：仓库 **Security** 标签页 → *Report a vulnerability*（推荐，可在线协作修复）；
2. 邮件：发送至仓库公开资料中的维护者邮箱，标题以 `[SECURITY] ypbin-iot-starter` 开头。

上报时请尽量包含：

- 受影响的模块与提交/版本（例如 `ypbin-iot-protocol-opcua` @ `aaa6021`）；
- 复现步骤或最小可复现样例（含 JDK 版本、设备/服务端形态、相关配置）；
- 影响面判断（能读到什么、能改成什么、是否需要能连上目标设备或中间人位置）；
- 若已知，附上缓解建议。

## 我们的处理节奏

- **48 小时内**确认收到并给出初步判断；
- 确认有效后，在修复合并前与你同步进展（如需，可约定协调披露窗口）；
- 修复合并后，在 `CHANGELOG.md` 中致谢（如你愿意具名）。

## 已知的安全设计边界（避免误报）

以下属**已知限制或有意设计**，欢迎讨论，但请先阅读依据：

- **OPC UA 信任模型**：信任目录只能放「自签证书」（= 精确 pin 该证书）或「自签 CA」
  （= 信任该 CA 签发的一切）。**放「CA 签发的叶子证书」当不了信任锚**（Milo 的行为，已实测）。
  另：`HOSTNAME` / `APPLICATION_URI` 两个可选校验的比对值取自**服务端自述**，
  **对主动 MITM 无防护** —— 真正的门闩只有信任列表本身。见 `docs/PROTOCOLS.md`。
- **Modbus RTU 无真实硬件验证**：仅协议层与串口配置校验，未在真实串口设备上验证过。
- **指标默认不落盘**：未提供 `MeterRegistry` 时指标走无操作实现并被丢弃（装配期会打 INFO），
  这不是「静默失败」而是明确的降级路径。
- **`ypbin-iot-test` 会传递 JUnit / AssertJ**：它是发布出去的测试基座（TCK），
  依赖作用域是 compile 属有意为之；**生产代码不应依赖它**。
- **明文协议本身没有机密性**：TCP 透传、Modbus（无 TLS 时）、MQTT 明文连接、
  OPC UA `None` 安全策略下的通信不加密——这是协议语义，不是框架缺陷；
  其中「配置了凭据却用 `None` 策略」会被**显式拒绝**，不会静默明文发送。

## 供应链与静态扫描

- CI 对 `master` 每个提交执行构建、架构约束、覆盖率门禁与**集成测试**；
- CodeQL（`security-and-quality`）在 push / PR / 每周定时运行，告警可在 Security 面板查看；
- 依赖更新由 Dependabot 按同族分组提 PR（见 `.github/dependabot.yml`）。
